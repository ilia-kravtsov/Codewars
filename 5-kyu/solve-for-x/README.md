<h2><a href=https://www.codewars.com/kata/59c2e2a36bddd2707e000079/train/javascript target="_blank">Solve For X</a></h2><h3>5 kyu</h3><h1 id="solve-for-x">Solve For X</h1><p>You will be given an equation as a string and you will need to <a href="https://www.mathplacementreview.com/algebra/basic-algebra.php#solve-for-a-variable" data-turbolinks="false" target="_blank">solve for X</a> and return x's value. For example: </p><pre><code class="language-javascript"><span class="cm-variable">solveForX</span>(<span class="cm-string">'x - 5 = 20'</span>); <span class="cm-comment">// Should return 25</span><span class="cm-variable">solveForX</span>(<span class="cm-string">'20 = 5 * x - 5'</span>); <span class="cm-comment">// Should return 5</span><span class="cm-variable">solveForX</span>(<span class="cm-string">'5 * x = x + 8'</span>); <span class="cm-comment">// Should return 2</span><span class="cm-variable">solveForX</span>(<span class="cm-string">'(5 - 3) * x = x + 2'</span>); <span class="cm-comment">// Should return 2</span></code></pre><pre style="display: none;"><code class="language-python"><span class="cm-variable">solve_for_x</span>(<span class="cm-string">'x - 5 = 20'</span>) <span class="cm-comment"># should return 25</span><span class="cm-variable">solve_for_x</span>(<span class="cm-string">'20 = 5 * x - 5'</span>) <span class="cm-comment"># should return 5</span><span class="cm-variable">solve_for_x</span>(<span class="cm-string">'5 * x = x + 8'</span>) <span class="cm-comment"># should return 2</span><span class="cm-variable">solve_for_x</span>(<span class="cm-string">'(5 - 3) * x = x + 2'</span>) <span class="cm-comment"># should return 2</span></code></pre><p>NOTES:</p><ul><li>All numbers will be whole numbers</li><li>Don't forget about the <a href="https://www.mathplacementreview.com/algebra/basic-algebra.php#order-of-operations" data-turbolinks="false" target="_blank">order of operations</a>.</li><li>If the random tests don't pass the first time, just run them again.</li></ul>