<h2><a href=https://www.codewars.com/kata/52744341faf5480173000bc7/train/javascript target="_blank">Streams - endless arrays</a></h2><h3>5 kyu</h3><p>The goal of this kata is to write a <code>Stream</code> - an endless sequence of elements. Such a <code>Stream</code> usually has two methods <code>head()</code>, requesting the first element of the current <code>Stream</code> and <code>tail()</code> to get the rest of the current <code>Stream</code> without the head.</p><p>An example use could be:</p><pre><code class="language-javascript"><span class="cm-keyword">function</span> <span class="cm-def">increment</span>(<span class="cm-def">n</span>) {<span class="cm-keyword">return</span> <span class="cm-variable-2">n</span> <span class="cm-operator">+</span> <span class="cm-number">1</span>};<span class="cm-keyword">function</span> <span class="cm-def">id</span>(<span class="cm-def">n</span>) {<span class="cm-keyword">return</span> <span class="cm-variable-2">n</span>};<span class="cm-keyword">function</span> <span class="cm-def">sqr</span>(<span class="cm-def">n</span>) {<span class="cm-keyword">return</span> <span class="cm-variable-2">n</span> <span class="cm-operator">*</span> <span class="cm-variable-2">n</span>};<span class="cm-keyword">const</span> <span class="cm-def">naturalNumbers</span> <span class="cm-operator">=</span> <span class="cm-keyword">new</span> <span class="cm-variable">Stream</span>(<span class="cm-number">0</span>, <span class="cm-variable">id</span>, <span class="cm-variable">increment</span>);<span class="cm-variable">console</span>.<span class="cm-property">log</span>(<span class="cm-variable">naturalNumbers</span>.<span class="cm-property">head</span>()) <span class="cm-comment">// 0</span><span class="cm-variable">console</span>.<span class="cm-property">log</span>(<span class="cm-variable">naturalNumbers</span>.<span class="cm-property">tail</span>().<span class="cm-property">head</span>()) <span class="cm-comment">// 1</span><span class="cm-variable">console</span>.<span class="cm-property">log</span>(<span class="cm-variable">naturalNumbers</span>.<span class="cm-property">tail</span>().<span class="cm-property">tail</span>().<span class="cm-property">head</span>()) <span class="cm-comment">// 2</span></code></pre><pre style="display: none;"><code class="language-coffeescript"><span class="cm-variable">increment</span> <span class="cm-punctuation">=</span> <span class="cm-punctuation">(</span><span class="cm-variable">n</span><span class="cm-punctuation">)</span> <span class="cm-operator">-&gt;</span> <span class="cm-variable">n</span> <span class="cm-operator">+</span> <span class="cm-number">1</span><span class="cm-variable">id</span> <span class="cm-punctuation">=</span> <span class="cm-punctuation">(</span><span class="cm-variable">n</span><span class="cm-punctuation">)</span> <span class="cm-operator">-&gt;</span> <span class="cm-variable">n</span><span class="cm-variable">sqr</span> <span class="cm-punctuation">=</span> <span class="cm-punctuation">(</span><span class="cm-variable">n</span><span class="cm-punctuation">)</span> <span class="cm-operator">-&gt;</span> <span class="cm-variable">n</span> <span class="cm-operator">*</span> <span class="cm-variable">n</span><span class="cm-variable">naturalNumbers</span> <span class="cm-punctuation">=</span> <span class="cm-keyword">new</span> <span class="cm-variable">Stream</span><span class="cm-punctuation">(</span><span class="cm-number">0</span><span class="cm-punctuation">,</span> <span class="cm-variable">id</span><span class="cm-punctuation">,</span> <span class="cm-variable">increment</span><span class="cm-punctuation">)</span><span class="cm-variable">console</span><span class="cm-punctuation">.</span><span class="cm-property">log</span> <span class="cm-variable">naturalNumbers</span><span class="cm-punctuation">.</span><span class="cm-property">head</span><span class="cm-punctuation">(</span><span class="cm-punctuation">)</span> <span class="cm-comment"># 0</span><span class="cm-variable">console</span><span class="cm-punctuation">.</span><span class="cm-property">log</span> <span class="cm-variable">naturalNumbers</span><span class="cm-punctuation">.</span><span class="cm-property">tail</span><span class="cm-punctuation">(</span><span class="cm-punctuation">)</span><span class="cm-punctuation">.</span><span class="cm-property">head</span><span class="cm-punctuation">(</span><span class="cm-punctuation">)</span> <span class="cm-comment"># 1</span><span class="cm-variable">console</span><span class="cm-punctuation">.</span><span class="cm-property">log</span> <span class="cm-variable">naturalNumbers</span><span class="cm-punctuation">.</span><span class="cm-property">tail</span><span class="cm-punctuation">(</span><span class="cm-punctuation">)</span><span class="cm-punctuation">.</span><span class="cm-property">tail</span><span class="cm-punctuation">(</span><span class="cm-punctuation">)</span><span class="cm-punctuation">.</span><span class="cm-property">head</span><span class="cm-punctuation">(</span><span class="cm-punctuation">)</span> <span class="cm-comment"># 2</span></code></pre><p>Implement a <code>Stream</code> object.</p><p>Further explenations:A <code>Stream</code> is constructed using three parameters:</p><pre><code class="language-javascript"><span class="cm-keyword">const</span> <span class="cm-def">naturalNumbers</span> <span class="cm-operator">=</span> <span class="cm-keyword">new</span> <span class="cm-variable">Stream</span>(<span class="cm-number">0</span>, <span class="cm-variable">id</span>, <span class="cm-variable">increment</span>);</code></pre><pre style="display: none;"><code class="language-coffeescript"><span class="cm-variable">naturalNumbers</span> <span class="cm-punctuation">=</span> <span class="cm-keyword">new</span> <span class="cm-variable">Stream</span><span class="cm-punctuation">(</span><span class="cm-number">0</span><span class="cm-punctuation">,</span> <span class="cm-variable">id</span><span class="cm-punctuation">,</span> <span class="cm-variable">increment</span><span class="cm-punctuation">)</span></code></pre><p>The second parameter is the so called output function. These functions controls the result of calling <code>head()</code>. The third parameter is the stepping function. It is very common to have just the <code>increment</code> function used for this.The first parameter is the start value. The first call to <code>head()</code> will return <code>&lt;output_function&gt;(&lt;start_value&gt;)</code></p><p>A call to <code>tail()</code> should always returns a new <code>Stream</code> object.</p>