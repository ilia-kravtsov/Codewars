<h2><a href=https://www.codewars.com/kata/5c4be30af3c54800102aa217/train/javascript target="_blank">New Function</a></h2><h3>5 kyu</h3><p>We all know that any integer number <code>n</code> is divisible by <code>1</code> and <code>n</code>. That is why these two numbers are not the actual divisors of any numbers. The function <strong>SOD(n)</strong> (sum of divisors) is defined as the summation of all the actual divisors of an integer number n. For example: <strong>SOD(24)</strong> = 2+3+4+6+8+12 = 35.</p><p>The function <strong>CSOD(n)</strong> (cumulative SOD) of an integer <code>n</code>, is defined as below:</p><img border="0" alt="Screenshot-3" src="https://i.ibb.co/b5M7n2n/Screenshot-3.png"><p>Given the value of <code>n</code>, your job is to find the value of <strong>CSOD(n)</strong>.</p><h1 id="random-test">Random Test</h1><ul><li><p><b>Number of Test Cases:</b> <code>2000</code></p></li><li><p><b>Range:</b> <code>0</code> to <code>100000000</code></p></li></ul>